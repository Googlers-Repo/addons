# Energized via Node.js

This library/module install energized.pro without any reboot

## Install 

```shell
grf add energized
```

> `grf` is an npm wrapper with additional configuration after the installation.